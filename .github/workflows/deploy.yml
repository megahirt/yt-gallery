name: Deploy to GitHub Pages

on:
  workflow_run:
    workflows: ["Fetch YouTube Videos"]
    types: [completed]
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    # When triggered by workflow_run, only proceed if the fetch succeeded.
    if: ${{ github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: gallery/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: gallery

      - name: Download videos.json artifact
        uses: dawidd6/action-download-artifact@v20
        with:
          workflow: fetch-videos.yml
          name: videos-json
          path: gallery/static
          if_no_artifact_found: warn

      - name: Ensure videos.json exists
        run: |
          if [ ! -f gallery/static/videos.json ]; then
            echo '[]' > gallery/static/videos.json
          fi

      - name: Build
        run: npm run build
        working-directory: gallery
        env:
          BASE_PATH: /yt-gallery
          VITE_PUBLISHED_VIDEOS_URL: ${{ vars.PUBLISHED_VIDEOS_URL }}
          PUBLIC_BUILD_VERSION: ${{ github.ref_name }}

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5.0.0
        with:
          path: gallery/build

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
